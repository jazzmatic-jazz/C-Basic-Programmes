#include <stdio.h>

 	int main ()
{
	int a,b,c;
	printf ("Enter the age of Friends:\n");
	scanf ("%d%d%d",&a,&b,&c);	
	
		if (a>b && a>c)
		
  		{ 		
  		
  		    //  if (a>c )
  			printf ("a is older than c\n");		}
  		
  		        if (b>a && b>c)
 		{			  
 		
 		    //	 if (b>c)
 	    		 printf ("b is older than c\n");	}
 		      
 		     	 if (c>a && c>b)
	  	{		 	
         		//	if (c>b)
		          printf ("c is older than b");	}
			   	  
	
	
	return 0;
}

